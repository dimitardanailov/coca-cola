# Ticket 01.2882103 - Decisions

Purpose: capture infra decisions, rationale, and impact.

## Decision Log

| ID | Date | Topic | Decision | Rationale | Impact | Status |
|---|---|---|---|---|---|---|
| D-001 | 2026-04-09 | Source artifacts | Use ARB and C4P PDFs as authoritative design inputs for stack parameterization. | Keeps decisions traceable to approved architecture and instance strategy. | All stack decomposition and variables must align to these sources. | accepted |
| D-002 | 2026-04-09 | Firestore security model | Treat backend `firebase-admin` access as IAM-controlled and not enforced by Firestore Security Rules. | Admin SDK uses service account credentials and bypasses Firestore Security Rules by design. Maria's PR concern is valid in this repository context. | Access control for backend operations must be reviewed in IAM/service account scopes; `firestore.rules` protect only client SDK flows if present. | accepted |
| D-003 | 2026-04-09 | Firestore rules retention | Keep `firestore.rules` as explicit policy artifact and defense-in-depth baseline while backend remains server-only. | Rules remain useful if client access is introduced later and document intended data ownership constraints. | Add follow-up validation to confirm no active client SDK access paths currently depend on these rules. | accepted |

## Open Questions

- Which resource groups should be isolated as separate OpenTofu modules?
- Which variables are global versus environment-specific?
- Which constraints from ARB conflict with current implementation assumptions?
- Are there any current web/mobile client SDK Firestore access flows in `promo-plus`?

## Follow-up Items

1. Add resource-by-type decomposition and map each group to a stack boundary.
2. Define variable catalog with defaults and per-environment overrides.
3. Add decision entries for each major trade-off during decomposition.
4. Publish infra analysis and decision summary in wiki.
5. Validate service account IAM roles for backend Firestore operations and record least-privilege recommendations.

## Repository Evidence

- `promo-plus/src/services/arbiter/index.js` initializes and uses `firebase-admin` (`initializeApp`, `getFirestore`).
- `promo-plus/package.json` includes `firebase-admin` dependency.
- No Firestore web/mobile client SDK imports were found under `promo-plus` during current search.
