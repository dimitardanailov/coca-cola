# Decisions — 02.2954338 Google Cloud Permissions

## Decision Log

### [2026-04-30] James Confirmed Product Alpha Permissions Payload Applied

**Status**: accepted

**Context**
James shared a written confirmation that the requested APIs, predefined-role references, and custom permission payload were applied to Product Alpha project `ai0015677-cdb-c4p-ngps-pdt-a`.

**Decision**
Treat Product Alpha permissions implementation as completed by James and move ticket execution from external-blocked mode to active validation mode:

- Keep Alpha as implemented/pending-verification evidence track.
- Continue remaining open items that are still unresolved (for example Firebase registration/console validation and cross-environment completion/governance responses).

**Evidence**
- James response on 2026-04-30 with full applied payload list:
	- API enablement array.
	- GitHub Actions service account predefined roles and custom permissions.
	- Data migration service account predefined/custom permissions.
	- GCP migration principal predefined/custom permissions.
	- Developer principal predefined/custom permissions.
	- Repository scope `promo-plus`.

**Consequences**
- External dependency on James for Product Alpha payload application is closed.
- Ticket remains open for validation and remaining environment/governance tracks.

---

### [2026-04-29] Ticket Execution Paused Pending Details From James, Marin, And Maria

**Status**: accepted

**Context**
Current execution stage has open blockers that require external stakeholder confirmation and detail completion.

**Decision**
Keep ticket in external-waiting mode until responses are received:

- Track pending details from James for Firebase registration completion verification.
- Track pending details from Marin and Maria for remaining open implementation/governance items.
- Resume active closure workflow immediately once responses arrive.

**Evidence**
- Dimitar status update on 2026-04-29: currently waiting for details from James or Marin and Maria.

**Consequences**
- Ticket remains open with explicit external dependency status.
- No closure claim is valid until pending stakeholder responses are captured in evidence.

---

### [2026-04-28] James Marked Firebase Work "Done" But Verification Shows Registration Still Missing

**Status**: accepted

**Context**
James replied `Done` after Firebase setup request. Dimitar re-verified immediately using both service enablement checks and Firebase API project lookup.

**Decision**
Treat current state as partial completion:

- `firebase.googleapis.com` enablement is complete on Alpha/Beta/Gamma.
- Firebase project registration/initialization is still incomplete because project lookups return `HTTP:404 NOT_FOUND`.
- Keep Firebase blocker open and track a second follow-up to James with concrete 404 evidence.

**Evidence**
- Enabled-services check output:
	- Alpha: `firebase.googleapis.com`
	- Beta: `firebase.googleapis.com`
	- Gamma: `firebase.googleapis.com`
- Firebase API check output:
	- `GET https://firebase.googleapis.com/v1beta1/projects/ai0015677-cdb-c4p-ngps-pdt-a` → `HTTP:404`
	- `GET https://firebase.googleapis.com/v1beta1/projects/ai0015677-cdb-c4p-ngps-pdt-b` → `HTTP:404`
	- `GET https://firebase.googleapis.com/v1beta1/projects/ai0015677-cdb-c4p-ngps-pdt-g` → `HTTP:404`
- Dimitar sent follow-up message to James with these findings and explicit request for Firebase project registration completion.

**Consequences**
- Do not close Firebase track on API enablement alone.
- Final closure condition remains Firebase project registration success + console visibility verification.

---

### [2026-04-28] Firebase Initialization Request Sent To James With Evidence

**Status**: accepted

**Context**
After confirming developer permissions are working and verifying Firebase is not initialized on Alpha (`firebase.googleapis.com` not enabled, Firestore databases empty), Dimitar sent a direct request to James to initialize Firebase on Alpha, Gamma, and Beta.

**Decision**
Treat the Firebase setup as an explicit outbound dependency request now in progress:

- Keep IAM remediation closed; do not open a new IAM ticket for this issue.
- Track James/Cloud-team Firebase initialization action as the active blocker-removal path.
- Resume validation only after confirmation that initialization is complete.

**Evidence**
- Outbound message to James (2026-04-28):
	- Developer access confirmed (Buckets, Cloud Run, Secrets).
	- Firebase API not enabled on `ai0015677-cdb-c4p-ngps-pdt-a`.
	- Request sent to initialize Firebase on Alpha, Gamma, Beta.

**Consequences**
- Current waiting state is external (James/Cloud team action).
- Next verification step is `firebase projects:list` and Firebase Console project visibility after James confirms setup.

---

### [2026-04-28] IAM Audit Revised — Developer Permissions Confirmed, Firebase Issue Is Initialization Gap

**Status**: accepted

**Context**
Initial finding suggested `roles/viewer` was the only role applied. Practical permission tests proved developer access is fully working. The Firebase CLI and Console failures have a different root cause.

**Decision**
Revise the earlier finding. Provisioning is correct:

- `roles/viewer` at project level is the base visibility binding; the custom developer role is applied at folder level and is effective.
- Firebase CLI and Firebase Console failures are caused by Firebase not being initialized on the Alpha/Gamma/Beta GCP projects, not by missing IAM permissions.
- Firebase initialization is a one-time admin action (`firebase projects:addfirebase`) that James or the Cloud team must perform.
- No IAM correction request is needed.

**Evidence**
- `gcloud run services list` → Listed 0 items (no PERMISSION_DENIED).
- `gsutil ls` → returned real bucket names including `c4p-ngps-product-alpha-staging-bucket`.
- `gcloud secrets list` → Listed 0 items (no PERMISSION_DENIED).
- `firebase projects:list` → error (Firebase not initialized on these projects).

**Consequences**
- Message to James should ask for Firebase initialization, not IAM correction.
- Firebase-dependent workflows remain blocked until initialization is done.
- All other developer workflows (Cloud Run, Storage, Secrets, Artifact Registry) are unblocked.

---

### [2026-04-28] IAM Audit Reveals Wrong Role Applied — SUPERSEDED

**Status**: superseded by [2026-04-28] IAM Audit Revised

**Context**
After GCP Console access was confirmed for Alpha/Gamma/Beta, Dimitar investigated why Firebase CLI and Firebase Console were failing. An IAM audit in Cloud Shell revealed that `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer@coca-cola.com` has only `roles/viewer` on the Alpha project (`ai0015677-cdb-c4p-ngps-pdt-a`). The custom developer role with the 15 agreed permissions was not applied.

**Decision**
Treat this as a provisioning error requiring James to correct the IAM binding:

- `roles/viewer` is not the agreed developer permission set.
- The agreed set includes 15 roles (see developer role audit in index) including `roles/firebase.viewer`, `roles/run.developer`, `roles/secretmanager.secretAccessor`, etc.
- Firebase Console and Firebase CLI failures are a direct consequence of the missing Firebase permissions.
- No user-side workaround exists — this requires a James-side correction.

**Evidence**
- Cloud Shell command output confirmed: `ROLE: roles/viewer` on `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer@coca-cola.com`.
- Custom developer role was defined and agreed in the permissions matrix but not applied.

**Consequences**
- Provisioning must be treated as incomplete despite the ticket being marked completed.
- James must be contacted with specific evidence (project ID, group name, current role, expected role) to correct the binding.
- All Firebase-dependent workflows remain blocked until the correct role is applied.

---

### [2026-04-28] GCP Access Confirmed, Firebase Console Visibility Is A Separate Problem

**Status**: accepted

**Context**
Dimitar confirmed GCP access to Alpha, Gamma, Beta projects is working via `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer`. However, the same projects are not visible in Firebase Console (console.firebase.google.com).

**Decision**
Treat Firebase Console visibility as a separate unresolved track from GCP Console access:

- GCP IAM access (including `roles/firebase.viewer`) does not automatically make a project appear in Firebase Console.
- Firebase Console only lists projects where Firebase has been explicitly initialized (added) to the GCP project. This is a one-time admin action.
- Two possible root causes:
  1. Firebase is not yet initialized on Alpha/Gamma/Beta GCP projects — requires admin to run `firebase projects:addfirebase` or add via Firebase Console with admin rights.
  2. Firebase is already initialized but the `firebase.viewer` binding has not propagated or was not applied.
- Resolution path: ask James (or Vijay) to confirm whether Firebase is initialized on those projects, or to perform that initialization step.
- If Firebase is not initialized, a new provisioning request may be required — this is not a user-side action.

**Evidence**
Dimitar message:
- Alpha, Gamma, Beta projects are accessible in GCP Console.
- Principal used: `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer`.
- Firebase Console does not show those projects after access was granted.
- `roles/firebase.viewer` is confirmed in the Developer role definition (see developer role audit in index).

**Consequences**
- Firebase Console visibility is now an explicit open validation item.
- If Firebase needs to be added to target projects, this requires a separate admin action by James or a new Cloud Service Request.
- Do not treat GCP Console access as proof of full environment readiness until Firebase Console visibility is also confirmed.

---

### [2026-04-27] Ticket Marked Completed But Visibility Is Partial — Keep Access Validation Open

**Status**: accepted

**Context**
Dimitar confirmed the access ticket is marked completed, but after logout/login only one project is visible in Google Cloud Console (`poc-missions-reqpoc00289`).

**Decision**
Treat provisioning as partially verified and keep the visibility-validation track open until expected project scope is explicitly confirmed:

- Do not close access validation based on ticket-completed status alone.
- Validate expected target-project visibility list against actual console/project listing.
- Confirm whether missing org/folder browse permission (project-list visibility) or IAM propagation delay is the cause.

**Evidence**
Dimitar message points:
- "The ticket is marked as completed."
- "I've access only to: poc-missions-reqpoc00289"
- "I already logged out and logged in."
- "Do you think is required to make anything else the project to be visible in my google cloud console?"

**Consequences**
- Access rollout remains in-progress until expected project visibility is proven.
- Follow-up with James/Vijay must include expected project list and permission path used for console discovery.

---

### [2026-04-27] James Reply To Follow-Up — Interim Tanya Path And Self-Service Developer Onboarding

**Status**: accepted

**Context**
James responded to Dimitar's follow-up questions about non-developer access and scaling Developer principal membership.

**Decision**
Adopt James response as the current operating guidance:

- Tanya/non-developer path: pending internal confirmation with Vijay; interim option is Developers group or DeveloperAdmin group (once created).
- Additional developers path: users must submit their own Cloud Service Request for Developer-group onboarding.
- James does not submit onboarding requests on behalf of users.

**Evidence**
James response points captured in team chat:
- "I will discuss internally with Vijay Goli, in the meantime she can be added to either the developers or developersAdmin group once it's created"
- "Who ever needs to be added to the developer group can submit a ticket here: https://prodcocacolaess.powerappsportals.com/cloudservicesrequest/. Please refer to my screenshot for the correct inputs. I don't need to submit a request for anyone."

**Consequences**
- Developer onboarding process is now explicitly self-service/requester-driven.
- Non-developer least-privilege design remains partially open pending Vijay discussion.
- Ticket must capture required screenshot input values to avoid failed Cloud Service Request submissions.

---

### [2026-04-27] Dimitar Follow-Up Questions — Read-Only Non-Developer Profile And Developer Group Expansion

**Status**: accepted

**Context**
After James confirmed Developer request submission and DeveloperAdmin-group path for Maria/Marin, Dimitar sent a follow-up response:

- Acknowledged and agreed with DeveloperAdmin proposal.
- Requested guidance for non-developer stakeholders (example: Tanya) who need read-only access only.
- Asked whether additional developers can be added to `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer` and indicated accounts will be shared if approved.

**Decision**
Treat this as a structured clarification request for access model completion:

- Require James recommendation for a non-developer read-only access profile.
- Require explicit yes/no and process guidance for adding additional developers to Developer principal.
- Keep additional-user submission pending until James confirms onboarding path.

**Evidence**
Dimitar message points captured in team chat:
- Thank-you for access ticket.
- Agreement with DeveloperAdmin model for Maria/Marin.
- Question on read-only permissions for non-developers.
- Question on expanding Developer principal membership.

**Consequences**
- Access model is not complete until non-developer read-only profile is defined.
- Developer onboarding scalability path remains pending James confirmation.

---

### [2026-04-27] James Provisioning Update — Developer Request Filed, DeveloperAdmin Group Required For Maria/Marin

**Status**: accepted

**Context**
James provided a follow-up access-provisioning update:

- Developer-group request for Dimitar has been filed through cloud request workflow.
- After internal alignment with Vijay, Maria and Marin should not be directly added through the current AppAdmin path.
- A separate cloud-team request is required to create a DeveloperAdmin group for Maria Dobreva and Marin Roglev.
- James stated AppAdmin currently carries Owner-level access and the goal is to reduce the number of individuals with that level.

**Decision**
Adopt this as current access rollout path:

- Treat Dimitar Developer-group access request as submitted and pending fulfillment.
- Use DeveloperAdmin-group creation as required path for Maria/Marin elevated needs.
- Preserve least-exposure principle by avoiding unnecessary expansion of AppAdmin (Owner-level) membership.

**Evidence**
James message details:
- Developer request link: https://prodcocacolaess.powerappsportals.com/mycasesandrequests/view_requests/?id=57cca83e-4e42-f111-9a90-000d3a35c8bb
- Stated need for separate cloud-team request for DeveloperAdmin group.
- Named users: Maria Dobreva, Marin Roglev.
- Rationale: reduce number of users with Owner-level AppAdmin access.

**Consequences**
- Access rollout is now split into two tracks: Developer onboarding and DeveloperAdmin-group creation.
- Ticket must track cloud-team request status for DeveloperAdmin creation before considering Maria/Marin access resolved.

---

### [2026-04-27] Dimitar Follow-Up Request — Starter Visibility Access Via Existing Principals

**Status**: accepted

**Context**
After James shared the two existing non-prod principals and custom-role delivery model, Dimitar sent a follow-up request asking for initial visibility access as a first operational step because team members currently have no access to any project.

**Decision**
Record this as an execution dependency request tied to implementation readiness:

- Requested starter access paths:
	- `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer`
	- `SEC-IDM-GCP-AI0015677-CDB_NonProd_AppAdmin`
- Keep request status as pending until James confirms process and assignment outcome.
- Treat assignee wording "Maria and Maria" as captured verbatim and require confirmation if a typo is suspected.

**Evidence**
Team chat details captured from Dimitar message:
- Thank-you response to James clarification.
- Request for starter visibility access via the two existing principals.
- Statement that team currently has no project access.
- Commitment to provide more specific instructions after deeper review.

**Consequences**
- Baseline access request is now explicitly tracked as a blocker for rapid iteration.
- Follow-up confirmation from James is required before treating access as provisioned.

---

### [2026-04-27] James Clarification — Existing AD Principals And Custom-Role-Only Delivery Model

**Status**: accepted

**Context**
James shared implementation constraints and current-state details for NGPS/Promo+ IAM:

- Current AD Group / IAM principal baseline includes two groups:
	- `SEC-IDM-GCP-AI0015677-CDB_NonProd_AppAdmin`
	- `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer`
- Developer additions are process-controlled via Cloud Service Request portal:
	- https://prodcocacolaess.powerappsportals.com/cloudservicesrequest/
- Permissions are managed via custom roles; predefined Google role IDs are not the final implementation input.
- Team must provide specific permission strings for Developer group.

**Decision**
Adopt James clarification as current execution model:

- Treat two listed AD principals as current operational baseline for non-prod path.
- Use custom permission arrays as the authoritative payload for implementation.
- Use predefined roles only as reference sources to derive permission strings.
- Prioritize Developer-group permission definition first, as explicitly requested by James.

**Evidence**
James message points captured in team chat:
- Existing principals listed by exact names.
- Cloud Service Request flow required for Developer onboarding.
- "We are managing permissions via a custom role" and "we will not be using google predefined roles".
- Request for specific Developer-group permissions and Cloud Run IAM docs reference.

**Consequences**
- Ticket deliverable changes from role-name lists to permission-level arrays.
- Any onboarding ask must include Cloud Service Request workflow, not only chat approval.
- Developer permission payload becomes immediate blocker-clearing artifact for implementation.

---

### [2026-04-27] James Response — Permission-Level Input Required (No Predefined-Role-Only Apply)

**Status**: accepted

**Context**
After Vijay approved an Alpha-only exception path, Dimitar sent James the requested Alpha group setup details with predefined role lists for `admin`, `developers`, and `Operational` groups, including requested members and CC visibility to Vijay.

James responded that implementation cannot proceed from predefined role names alone.

**Decision**
Adopt James's implementation constraint as a hard input requirement for this ticket:

- For Alpha, provide explicit permission-level lists (custom permission definitions) per group.
- Predefined role names remain reference material, but are not sufficient as final implementation payload.
- Keep Gamma/Prod policy unchanged and Beta pending explicit governance response.

**Evidence**
Team chat evidence:
- Dimitar request to James:
	- Create Alpha groups: `admin`, `developers`, `Operational`.
	- Requested members: `admin` (Marin, Maria), `developers` (Dimitar), `Operational` (Tanya).
	- Included full predefined role sets for all three groups.
- James response:
	- "I need specific permissions as we are not applying google predefined roles"
- CC context: Vijay Goli.

**Consequences**
- Ticket now has a concrete implementation blocker/resolution path: permission-level mapping must be produced.
- Requirements and next steps must prioritize Alpha permission-level deliverable before marking exception implementation ready.

---

### [2026-04-27] Vijay Response — Alpha Exception Approved, Beta Pending

**Status**: accepted

**Context**
After the team chat request for Alpha/Beta additional permissions, Vijay provided governance clarification and a scoped response.

**Decision**
Adopt Vijay's response as the current governance outcome:

- Alpha project: exception approved for additional developer-role permissions.
- Beta project: no approval in this response; keep baseline policy until explicitly confirmed.
- Gamma and Prod: unchanged under Sunil policy and CI/CD governance model.

**Evidence**
Vijay response points captured in team chat:
- Intent is to keep infrastructure fully aligned with repository code.
- CLI changes in GCP are difficult to reliably track and can cause drift from source-controlled state.
- Exception can be allowed for Alpha project.
- James can add required permissions to developer role to support Alpha.

**Consequences**
- Permissions design now has a written governance response for the exception request.
- Role-model updates must be scoped to Alpha unless a separate written Beta approval is received.
- Ticket tracking must now focus on exact Alpha permission deltas and implementation/audit controls.

---

### [2026-04-27] Alpha/Beta Additional-Permissions Exception Request Sent To James (CC Vijay)

**Status**: accepted

**Context**
After internal alignment with Maria, Dimitar sent a group-chat message to James requesting guidance on a potential exception model for lower environments. The request keeps Sunil policy strict for Gamma/Prod and asks whether Alpha/Beta can allow additional team permissions for faster implementation and troubleshooting.

**Decision**
Treat the chat message as an official governance request artifact for this ticket:

- Exception request scope: Alpha and Beta only.
- Baseline policy remains unchanged for Gamma and Prod unless governance issues a new written decision.
- No final role assignment change will be marked complete until a written response is received from James/Vijay governance path.

**Evidence**
Chat details:
- Author: Dimitar Danailov
- Addressee: James Moorman
- CC visibility: Vijay Goli
- Participants captured in group: Dimitar Danailov, Marin Roglev, Maria Dobreva, Tanya Tanevska, James Moorman, Vijay Goli
- Message intent:
	- Thank James for prior session.
	- Request possible Alpha/Beta exception with additional permissions for Maria and Marin.
	- Confirm Gamma/Prod should remain aligned with current governance model.
	- Ask for policy-alignment guidance.

**Consequences**
- Ticket now has a clear escalation artifact for Alpha/Beta exception review.
- Written outcome is now partially resolved: Alpha approved, Beta pending explicit confirmation.

---

### [2026-04-27] Access Provisioning Confirmation And Data-Move Test From Tanya Email (2026-04-23)

**Status**: accepted

**Context**
Tanya Tanevska shared an approval email confirming that access provisioning will be handled after a call with Sunil and requested timeline confirmation from James and Vijay. The same email adds a planning requirement to test moving production data between environments.

**Decision**
Add Tanya's email outcomes as explicit ticket facts and planning requirements:

- Treat access provisioning confirmation as an execution dependency tracked in this ticket.
- Include a work item to test production-data movement between environments.
- Keep this execution aligned with Sunil security governance and CI/CD service-account model.

**Evidence**
Email details:
- From: Tanya Tanevska (`ttanevska@coca-cola.com`)
- Sent: 2026-04-23 10:52 AM
- Subject: `Approved RE: Promo+/NGPS GCP Environment`
- Key statements:
	- Accesses will be provisioned (confirmed in call with Sunil).
	- Add plan item for testing production-data movement between environments.
	- Request to James and Vijay for provisioning confirmation timeline.

**Consequences**
- Ticket planning now includes provisioning-status tracking and production-data test scope.
- Timeline and owner clarity become mandatory before marking permissions design ready for implementation.

---

### [2026-04-27] Security Governance Baseline From Sunil Email (2026-04-23)

**Status**: accepted

**Context**
Policy email from Sunil Ramakrishnappa (Senior Director, US-based leadership chain for this external team context) sets explicit security and governance constraints for Promo+/NGPS GCP access.

**Decision**
Adopt the email policy as a hard constraint for this ticket:

- No admin access for developers or tech leads across GCP environments.
- IaC and CI/CD service accounts are the primary execution identities for deployments.
- Permission gaps must be escalated through Vijay's team and coordinated with Cloud team / GCP experts.
- Fixes should be applied at the permission-model level, not via broad human admin grants.

**Evidence**
Email details:
- From: Sunil Ramakrishnappa (`sramakrishnappa@coca-cola.com`)
- Sent: 2026-04-23 9:15 PM
- Subject: `RE: Approved RE: Promo+/NGPS GCP Environment`
- Key statement: "As per TCCC/C4P security policies and guidelines, we will not be issuing admin access to developers or tech leads across our GCP environments."

**Consequences**
- Role design must keep user permissions development-scoped and avoid human admin elevation.
- Deployment capability should be implemented through CI/CD service accounts and WIF-based identity flow.
- Any request for additional access must include technical justification and follow the escalation path.

---

### [2026-04-27] DevOps Execution Model Confirmed With Vijay Goli

**Status**: accepted

**Context**
Meeting held with Maria Dobreva, James Moorman, and Vijay Goli (manager of James). Vijay clarified the operating model for delivery and access boundaries.

**Decision**
Use a CI/CD-first deployment model for Promo Plus permissions execution:

- Deployments are executed through GitHub Actions CI/CD pipelines.
- Workflow Identity Federation is used for CI/CD identity flow.
- Pipeline service account receives deployment roles required for rollout.
- Individual user access remains limited to development activities.
- The canonical handoff artifact to DevOps execution is [APIs, Predefined Roles, & Custom Permissions](../../promo-plus-core-infrastructure/.context/gcp-iam/apis-predefined-roles-custom-permissions.md).

**Evidence**
Message shared by Vijay before the meeting:
"We follow a DevOps process to bootstrap projects, where deployments are handled through CI/CD GitHub Actions. The service account used in the pipeline is granted the necessary roles for deployments, while individual user access is limited to development activities."

**Consequences**
- Permission design must distinguish deployment service-account roles from individual developer roles.
- Permissions matrix must explicitly document required service accounts and exact IAM permissions for WIF-based CI/CD deployment.
- Ticket outputs should be implementation-ready for James with minimal back-and-forth.
- Access reviews should focus on least privilege for both pipeline and user identities.

---

### [2026-04-27] `roles/resourcemanager.projectIamAdmin` Removed From Admin AD Group

**Status**: superseded

**Context**
`roles/resourcemanager.projectIamAdmin` was initially included in the admin AD group role list. This role allows any holder to set or replace any IAM policy on the project, including granting themselves `roles/owner`. It is a privilege escalation vector with no ceiling and is incompatible with a human-user AD group under any least-privilege or TCCC/C4P governance model.

Three converging constraints require its removal from human AD groups:

| Constraint | Source |
|---|---|
| TCCC/C4P policy — no admin for developers or tech leads | Sunil email, 2026-04-23 |
| DevOps model — resource provisioning is an automation function, not a human task | Vijay meeting, 2026-04-27 |
| Least-privilege — human admins have no legitimate need to rewrite project IAM policies | `01.requirements.md` constraints |

**Decision**
Initial proposal was to remove `roles/resourcemanager.projectIamAdmin` from the admin AD group. This proposal is now superseded and on hold.

- This role belongs exclusively on the **GitHub Actions CI/CD service account** (WIF identity), where OpenTofu applies IAM bindings during provisioning runs.
- Human admin AD group members operate services, not IAM policy infrastructure.
- Pattern evidence: MIXCODES completed sections in the permissions matrix already place this role on service accounts, not AD groups.

**Evidence**
- MIXCODES BETA/GAMMA/PROD service account ROLE arrays in `apis-predefined-roles-custom-permissions.md` include `roles/resourcemanager.projectIamAdmin` on the pipeline identity.
- No legitimate admin operator task requires directly modifying project-level IAM policy bindings outside of an IaC pipeline run.

**Consequences**
- No removal is currently treated as approved.
- Current source matrix remains at 24 admin roles until written governance outcome is received for the Alpha/Beta exception request.
- Final role model remains pending James/Vijay written response.

---

### [2026-04-27] `roles/serviceusage.serviceUsageAdmin` And `roles/iam.roleAdmin` Removed From Admin AD Group

**Status**: superseded

**Context**
Following the same governance review that removed `roles/resourcemanager.projectIamAdmin`, two further roles were identified as incompatible with a human AD group under TCCC/C4P policy and the IaC-first operating model.

- `roles/serviceusage.serviceUsageAdmin` enables and disables GCP APIs at the project level. This is infrastructure state — it is managed by `google_project_service` resources in OpenTofu and executed by the CI/CD pipeline. A human enabling or disabling APIs manually bypasses IaC governance and violates Sunil's explicit policy that the environment is governed through the GCP hierarchy and IaC.
- `roles/iam.roleAdmin` creates, modifies, and deletes custom IAM roles on the project. Custom role definitions must be IaC-managed. Held by a human, this role allows crafting a role that grants any permission and assigning it — a privilege escalation path with the same risk profile as `roles/resourcemanager.projectIamAdmin`.

**Decision**
Initial proposal was to remove both roles from the admin AD group. This proposal is now superseded and on hold.

- Both belong exclusively on the **GitHub Actions CI/CD service account** (WIF identity) where OpenTofu manages API enablement and custom role definitions.
- Human admin AD group members operate deployed services, not infrastructure policy or configuration.

**Evidence**

| Constraint | Source |
|---|---|
| TCCC/C4P policy \u2014 environment governed through IaC and GCP hierarchy | Sunil email, 2026-04-23 |
| DevOps model \u2014 service accounts hold required access for resource creation and configuration | Sunil email, 2026-04-23 |
| Least-privilege \u2014 API toggles and custom role authoring are pipeline functions, not human admin tasks | `01.requirements.md` constraints |

**Consequences**
- No removal is currently treated as approved.
- Current source matrix remains at 24 admin roles until written governance outcome is received for the Alpha/Beta exception request.
- Final role model remains pending James/Vijay written response.

---

### [2026-04-24] IAM Role Audit — Admin And Developer Groups

**Status**: accepted

**Context**
After the initial role list was drafted, a second-pass audit was performed to verify completeness by mapping each role against wired services in `modules/` and cross-referencing the MIXCODES reference data already in the permissions matrix.

**Admin — 24 roles (current baseline; exception-governance outcome pending)**

| Role | Service covered |
|---|---|
| `roles/resourcemanager.projectIamAdmin` | IAM binding management |
| `roles/serviceusage.serviceUsageAdmin` | Enable/disable GCP APIs |
| `roles/iam.roleAdmin` | Create and manage custom roles |
| `roles/iam.serviceAccountAdmin` | Create/delete service accounts |
| `roles/iam.serviceAccountUser` | Attach service accounts to resources |
| `roles/compute.networkAdmin` | VPC, subnets, routes |
| `roles/compute.securityAdmin` | Firewall rules, security policies |
| `roles/compute.loadBalancerAdmin` | Load balancer resources |
| `roles/networkservices.admin` | URL maps, NEGs, backend services |
| `roles/vpcaccess.admin` | VPC connectors (separate API from compute) |
| `roles/run.admin` | Cloud Run — includes invoker permissions |
| `roles/artifactregistry.admin` | Container image repositories |
| `roles/storage.admin` | GCS buckets and objects |
| `roles/datastore.owner` | Firestore / Datastore |
| `roles/bigquery.admin` | BigQuery datasets and jobs |
| `roles/firebase.admin` | Firebase configuration |
| `roles/secretmanager.admin` | Secret Manager |
| `roles/cloudscheduler.admin` | Cloud Scheduler jobs |
| `roles/cloudarmor.admin` | Cloud Armor security policies |
| `roles/certificatemanager.owner` | TLS certificates |
| `roles/redis.admin` | Memorystore Valkey |
| `roles/logging.admin` | Cloud Logging |
| `roles/monitoring.admin` | Cloud Monitoring |
| `roles/iap.admin` | Services protected by Identity-Aware Proxy |

**Developer — 15 roles: all confirmed required**

| Role | Covers |
|---|---|
| `roles/run.developer` | Deploy Cloud Run revisions |
| `roles/iam.serviceAccountUser` | Attach service account to Cloud Run at deploy time |
| `roles/vpcaccess.user` | Use a VPC connector when deploying Cloud Run |
| `roles/artifactregistry.writer` | Push container images (includes pull) |
| `roles/storage.objectAdmin` | GCS object CRUD |
| `roles/datastore.user` | Firestore read/write |
| `roles/bigquery.dataEditor` | BigQuery data CRUD |
| `roles/bigquery.jobUser` | Run BigQuery jobs |
| `roles/run.invoker` | Run cloud endpoints from local machines |
| `roles/secretmanager.secretAccessor` | Read secret values |
| `roles/logging.logWriter` | Write application logs |
| `roles/logging.viewer` | View logs for debugging |
| `roles/monitoring.metricWriter` | Write custom metrics |
| `roles/monitoring.viewer` | View dashboards and alerts |
| `roles/firebase.viewer` | View Firebase configuration |

**Confirmed with Maria — two items were resolved before list was sent to James:**

1. `roles/iap.admin` for admin: confirmed — Promo Plus services run as private microservices protected by IAP. Included in admin baseline (24 total while exception-governance outcome is pending).
2. `roles/run.invoker` for developers: confirmed — developers invoke Cloud Run endpoints locally during development. Added to developers (15 total).

**Consequences**
- Role list includes IAP-protected service handling and local endpoint invocation needs.
- List is ready to pass to James for application.

---

### [2026-04-24] James Moorman Collaboration Model

**Status**: accepted

**Context**
James Moorman is the source author of the permissions matrix artifact and an external collaborator from the 0801 Martech team. During planning, a question arose about whether James holds decision authority over Promo Plus IAM and permissions choices.

**Decision**
James is a technical reference source, not a lead or decision maker for Promo Plus permissions work.

- He provides technical details: which APIs to enable, which roles to apply, which accounts are needed.
- He does not approve, block, or own Promo Plus IAM decisions.
- Promo Plus team (Maria, Marin) owns all decisions and execution.
- His source document style and structure should be preserved for consistency, not because he is a gatekeeper.

**Consequences**
- Promo Plus team can proceed with IAM role decisions independently.
- James can be consulted as a reference but does not need to sign off.
- ExternalTeams.md updated to reflect this collaboration model.

---

### [2026-04-24] Baseline Access Group Policy For Permissions Matrix

**Status**: accepted

**Context**
The permissions matrix file needed a clear, reusable access-control baseline before detailed API and IAM role mapping for NGPS/Promo+.

**Options Considered**

| Option | Pros | Cons |
|--------|------|------|
| Single table combining groups and environment permissions | Simple initial shape | Repeats group metadata many times; harder to scan |
| Split into group details + permission matrix tables | Better readability, less duplication, easier maintenance | Slightly longer section |

**Decision**
Use a split-table model at the top of the permissions file:
- Table 1: group details (`admin`, `developers`, `Operational`, members empty)
- Table 2: permission matrix by environment (Alpha, Betta, Gamma, Prod)

Use emoji status in the matrix (`✅` / `❌`) for fast visual scanning.

Baseline access rule:
- `admin`: read/write across all environments
- `developers`: read/write in Alpha/Betta, read-only in Gamma/Prod
- `Operational`: read-only across all environments

**Change Logic**
- Why this approach: Enforces least privilege, improves readability, and clearly protects higher environments.
- Alternatives rejected because: A single denormalized table duplicated metadata and made review slower.

**Consequences**
- NGPS/Promo+ role mapping now has a defined governance baseline.
- Gamma and Prod write access is explicitly restricted to `admin`, reducing operational and billing risk.
- Next iterations should focus on API enablement and predefined/custom role mapping per environment.

---

### [2026-04-24] Source Authorship And Style Preservation

**Status**: accepted

**Context**
The permissions matrix artifact in `promo-plus-core-infrastructure/.context/gcp-iam/apis-predefined-roles-custom-permissions.md` is authored by James Moorman. Promo Plus additions are needed for team readability and governance, but they must remain compatible with the source document style.

**Options Considered**

| Option | Pros | Cons |
|--------|------|------|
| Rewrite sections with a new style | Could optimize for one audience | Breaks consistency with source artifact and author expectations |
| Additive updates that preserve source style | Keeps continuity, easier review by source author and team | Requires stricter editorial discipline |

**Decision**
Use additive updates only, while preserving the structure and writing style established by James in the source artifact.

Scope boundary for this ticket:
- Keep James's existing document flow and naming.
- Add Promo Plus-specific readability sections only when they remain compatible with that style.

**Change Logic**
- Why this approach: Reduces review friction and prevents style drift in a shared cross-team artifact.
- Alternatives rejected because: A style rewrite would create unnecessary divergence from the source owner's document conventions.

**Consequences**
- Ticket outputs must explicitly note source authorship and style alignment.
- Future AI edits should be reviewed against style compatibility, not only technical correctness.

---

### [2026-04-24] Operator-First Scope Snapshot For AWS-to-GCP Migration

**Status**: accepted

**Context**
The current Infrastructure Services - Scope Snapshot is helpful but still module-centric. For migration execution, Admin needs an operator-first view that includes foundational domains beyond current root module wiring. The project also carries AWS legacy history via ngps-global and requires careful migration interpretation.

**Options Considered**

| Option | Pros | Cons |
|--------|------|------|
| Keep current snapshot focused only on wired OpenTofu modules | Simple and low-maintenance | Misses operational foundations and can block independent Admin operation |
| Expand snapshot to operator-first domains while keeping GCP target focus | Better run-time clarity and migration coverage | Requires additional curation and validation |

**Decision**
Expand Infrastructure Services - Scope Snapshot with operator foundations (including file storage, service principals/accounts, and logging/monitoring/reporting), while preserving GCP-target orientation.

Use ngps-global as migration-reference evidence only:
- It helps detect missing domains from AWS legacy behavior.
- It must not be treated as a direct permission template for GCP target operations.

**Change Logic**
- Why this approach: Improves operational independence for Admin and reduces reliance on ad hoc context escalation.
- Alternatives rejected because: A module-only snapshot can hide critical operational gaps in long-running migration programs.

**Consequences**
- Future snapshot updates must validate both migration completeness and operator usability.
- Admin workflows should avoid AWS-era access fallback assumptions.

---

### [2026-04-16] AD Group Structure Proposal From James Email

**Status**: accepted

**Context**
James Moorman sent an email asking Vijay for guidance on how to assign TCCC employees to AD groups for GCP access. At the time, the permissions matrix was being built and Vessela had provided a user list identifying project owners and developers but no explicit AD group mapping had been confirmed.

**Decision**
Capture James's proposed AD group mapping as the working model that was later confirmed through meetings and subsequent emails:

- Project owners → admin AD group.
- Developers → developer AD group.
- AD group structure aligns with the three-group baseline (`admin`, `developers`, `Operational`) adopted in this ticket.
- Vijay is the governance authority for confirming this mapping.

**Evidence**
Email details:
- From: James Moorman (`jmoorman@coca-cola.com`)
- Sent: 2026-04-16 10:01 PM
- Subject: `Re: Promo+/NGPS GCP Environment`
- To: Vessela Velinova-Slancheva, Vijay Goli
- Cc: Marin Roglev, Tanya Tanevska, Maria Dobreva
- Key statements:
  - Asks Vijay to confirm: project owners → admin AD group; developers → developer AD group.
  - Asks Vessela for required developer AD group permissions list.
  - Notes Vessela's wiki edit access was blocked; James working to resolve it.

**Consequences**
- AD group model is traceable to this email as its earliest source in the thread.
- Developer permissions list request was the open ask that drove subsequent work in this ticket.
- Wiki access blocker for Vessela is considered closed once permissions were provisioned.

---

### [2026-04-27] Operational Group Role List — Pending Audit

**Status**: open

**Context**
The Operational group (10 roles) is referenced by count throughout the ticket but the actual role list has never been explicitly documented, confirmed, or audited in this ticket. Admin (24 roles) and Developer (15 roles) both have full audited tables; Operational does not.

**Decision**
Pending: the Operational role list must be explicitly documented and audited before the permissions matrix is considered complete.

- The 10 roles must be enumerated as a confirmed read-only set covering the same platform services as admin and developers.
- Each role must be justified against the least-privilege constraint (read-only scope).
- The list must be reviewed by Maria before the permissions matrix is handed to James for implementation.

**Consequences**
- Permissions matrix hand-off to James is blocked until Operational role list is confirmed.
- New acceptance criterion added to `01.requirements.md` to track this explicitly.

---

### [2026-04-27] `roles/datastore.owner` For Admin — Acceptability Check

**Status**: open

**Context**
`roles/datastore.owner` is included in the admin group's 24 roles. This role grants full control of Firestore / Datastore including schema and index management. The least-privilege constraint requires confirming that no narrower predefined role (e.g. `roles/datastore.user` + `roles/datastore.indexAdmin`) would be sufficient for admin operations in this project.

**Decision**
Pending: confirm with Maria and James whether `roles/datastore.owner` is the correct minimum role for admin, or whether a narrower custom role is required.

Options to evaluate:

| Option | Granularity | Risk |
|---|---|---|
| Keep `roles/datastore.owner` | Simple, full admin coverage | Grants more than needed if admins don't manage indexes/schema |
| Replace with `roles/datastore.user` + index admin | Least-privilege | Requires a custom role or two-role binding |

**Consequences**
- If replaced with a custom role, this becomes a new custom-role delivery item in the permissions matrix.
- Resolution required before permissions matrix is sent to James.
- New acceptance criterion added to `01.requirements.md` to track this explicitly.

---

### [2026-04-27] Named Approver For Permissions Matrix Sign-Off — Unresolved

**Status**: open

**Context**
There is no confirmed named approver for the permissions matrix before it is considered done. After the April 27 meeting with Vijay and James, the governance chain is clearer (Sunil → Vijay → James for execution; Tanya → Maria for Promo Plus ownership), but no one has been explicitly designated as the sign-off authority for the completed permissions matrix.

**Decision**
Pending: identify and confirm the named approver before closing the ticket.

Candidates:
- Maria Dobreva — team lead and ticket owner; best positioned as Promo Plus sign-off.
- Vijay Goli — execution authority for DevOps/IAM side; may need to confirm service-account and CI/CD role assignments.
- Both may be needed: Maria for Promo Plus role coverage; Vijay for TCCC/C4P policy compliance.

**Consequences**
- Without a named approver, the ticket has no definition of done from a governance perspective.
- New acceptance criterion added to `01.requirements.md` to track this explicitly.

---
