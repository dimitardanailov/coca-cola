# Requirements — 02.2954338 Google Cloud Permissions

## Problem Statement

The permissions matrix markdown for GCP APIs, Predefined Roles & Custom Permissions has an empty NGPS/Promo+ section. The baseline governance layer is now added (group details table, permission matrix table, and explanatory bullets). Next, the NGPS/Promo+ section must be filled with correct API, permission, and IAM role mappings per environment.

The Infrastructure Services - Scope Snapshot currently improves readability but is not yet complete for operator execution. It needs explicit coverage of foundational operational domains (file storage, service principals/accounts, logging/monitoring/reporting) so Admin can operate in GCP without AWS-era dependency assumptions.

Markdown source: [promo-plus-core-infrastructure/.context/gcp-iam/apis-predefined-roles-custom-permissions.md](../../promo-plus-core-infrastructure/.context/gcp-iam/apis-predefined-roles-custom-permissions.md)

## Acceptance Criteria

- [ ] All GCP APIs used by NGPS/Promo+ are listed.
- [ ] Each API is mapped to the minimum required permissions (least-privilege principle).
- [ ] Principal role model is aligned with James clarification: GitHub Actions SA uses predefined roles; Data Migration SA uses custom role only; GCP Migration Principal and Developer Principal use custom role only.
- [ ] Each principal permission set is represented in the matrix according to the agreed role model without mixed-role ambiguity.
- [x] Baseline access groups section is present with split tables (group details and permission matrix).
- [x] Permission matrix readability is improved using emoji status values.
- [x] Source authorship is documented: James Moorman is the source author of the permissions matrix artifact.
- [ ] Role assignments are specified per environment (Alpha / Betta / Gamma / Prod and regional environments where applicable).
- [ ] Permissions matrix markdown NGPS/Promo+ section is complete and reviewed by the owner.
- [ ] Work item for testing production-data movement between environments is added to the plan and has clear owner/scope.
- [x] New additions preserve the source document style and structure used by James.
- [x] Infrastructure Services - Scope Snapshot explicitly includes file storage, service principals/accounts, and logging/monitoring/reporting.
- [x] Scope Snapshot entries distinguish GCP target services vs migration-reference sources.
- [x] Scope Snapshot supports Admin operational decisions without requiring AWS resource access requests as a fallback.
- [x] CI/CD service account roles and required IAM bindings are explicitly listed in the permissions matrix.
- [ ] Access provisioning ETA confirmed by James and Vijay following Tanya's April 23 approval.
- [ ] `roles/datastore.owner` acceptability confirmed — narrower custom role used if required by least-privilege review.
- [ ] Operational group role list (10 roles) is explicitly documented and audited.
- [ ] Named approver for permissions matrix sign-off is identified and confirmed.
- [x] Written governance outcome received from Vijay for the exception request: Alpha approved as special case; Gamma/Prod unchanged.
- [ ] Beta environment exception decision explicitly confirmed in writing (approved or rejected).
- [ ] Alpha exception controls finalized in writing (scope of permissions, duration/expiry, approval and audit trail).
- [x] Alpha implementation payload includes explicit permission-level lists for Developer group as required by James; predefined role names are used only as reference.
- [ ] Existing non-prod principal baseline (AppAdmin / Developer) is reflected in delivery notes and group mapping assumptions.
- [x] Cloud Service Request submitted for Dimitar Developer-group onboarding (request link captured in ticket evidence).
- [x] James confirms Developer permission-level payload is actionable for execution.
- [x] James confirms outcome of starter visibility-access request via existing principals (Developer/AppAdmin) — GCP Console access to Alpha, Gamma, Beta confirmed 2026-04-28.
- [x] Ticket completion evidence is matched by expected Google Cloud Console project visibility scope (not only `poc-missions-reqpoc00289`).
- [x] Firebase initialization request sent to James with evidence (`firebase.googleapis.com` not enabled on Alpha project).
- [x] `firebase.googleapis.com` is confirmed enabled on Alpha, Beta, Gamma after James response.
- [ ] Firebase initialized on Alpha, Gamma, Beta GCP projects by James or Cloud team (`firebase projects:addfirebase` or equivalent).
- [ ] Firebase Console (console.firebase.google.com) visibility confirmed for Alpha, Gamma, Beta projects under `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer` principal.
- [ ] Pending detail responses from James, Marin, and Maria are captured and reconciled in ticket evidence.
- [x] James provided explicit principal-by-principal role model clarification (predefined vs custom) for implementation.
- [ ] Cloud-team request for DeveloperAdmin group creation (Maria Dobreva, Marin Roglev) is submitted and approved.
- [ ] DeveloperAdmin group membership for Maria/Marin is confirmed and access path is documented.
- [ ] James provides explicit read-only access recommendation/profile for non-developer stakeholders (example: Tanya).
- [x] James confirms additional developers can be added to `SEC-IDM-GCP-AI0015677-CDB_NonProd_Developer` via requester-submitted Cloud Service Request process.
- [ ] Required Cloud Service Request input values from James screenshot are documented in ticket execution notes.

## Constraints

- Least-privilege: do not grant broader roles than what the service actually needs.
- Must align with GCP IAM best practices.
- Role assignments must be environment-aware (no prod-level roles on dev).
- Per TCCC/C4P policy: do not issue admin access to developers or tech leads across GCP environments.
- Deployment permissions must be modeled through CI/CD service accounts (WIF/service-account path), not broad human-user admin grants.
- Preserve source artifact style: future AI additions should match the structure and writing style already established in the permissions matrix by James.
- Use ngps-global as migration-reference evidence, not as a direct permission template for GCP operations.

## Non-Goals

- Terraform implementation of the roles (separate ticket).
- Other services in the same permissions matrix file beyond NGPS/Promo+.
- Individual user-to-group provisioning (deciding who is added to each AD group) — role definitions for each group are in scope; the provisioning act is not.
- Reproducing AWS service access patterns one-to-one in GCP without migration validation.

## Reviewer Focus

- Are predefined roles truly least-privilege, or do they grant excess permissions?
- Are any custom roles actually needed, or can predefined roles cover all cases?
- Is the environment differentiation correct and consistent?
- Does the scope snapshot include all operator-critical foundations needed for day-to-day Admin operation?
- Do role assignments respect policy by avoiding admin grants to developers/tech leads and keeping deployment elevation on service accounts?

---

## Output Model

**Facts**
- Permissions matrix markdown exists with a dedicated NGPS/Promo+ section to fill in.
- Baseline group model is documented: `admin`, `developers`, `Operational`.
- Write access to Gamma and Prod is restricted to `admin` in baseline policy.
- Source author of the permissions matrix artifact: James Moorman (`jamesMoorman`).
- Target service: NGPS/Promo+ running on GCP.
- Infrastructure source: `promo-plus-core-infrastructure/`.
- ngps-global provides AWS baseline context for migration comparison.

**Assumptions**
- Environments are dev, staging, and prod. `?` — confirm with team.
- Service uses Workload Identity Federation for CI/CD with dedicated deployment service accounts. Exact service accounts and permissions must be explicitly listed in the permissions matrix.
- First migration attempt by another team was not successful. `?` — source: stakeholder statement, needs archival evidence link.
- Existing environments Alpha/Gamma/Prod are already provisioned. `?` — source: stakeholder statement, needs infra evidence link.

**Unknowns**
- Exact list of GCP APIs consumed by NGPS/Promo+. `?`
- Whether any custom roles are already defined in the project. `?`
- Who reviews and approves the permissions matrix content before it is considered done. `?`
- Which operator domains are still missing from Scope Snapshot after current iteration. `?`
- Access provisioning ETA confirmation from James/Vijay after Tanya's approval note. `?`
- Beta decision from governance path (approved/rejected) after Vijay's Alpha-only response. `?`
- Final written control model for Alpha exception (scope, duration, approvals, audit). `?`
- Exact permission-level mapping for Alpha Developer group that James can apply without predefined-role translation. `?`
- Whether AppAdmin principal needs any change in this iteration or only Developer principal is in scope for immediate implementation. `?`
- ETA and owner for cloud-team request to create DeveloperAdmin group for Maria/Marin. `?`
- Required permission/profile shape for non-developer read-only access (for example Tanya). `?`
- Final Vijay-confirmed recommendation for non-developer placement (Developers vs DeveloperAdmin vs read-only profile). `?`
- Exact Cloud Service Request input values from James screenshot for Developer onboarding submissions. `?`
- Which additional projects should be visible for current principal after provisioning completion, beyond `poc-missions-reqpoc00289`. `?` — partially resolved: Alpha, Gamma, Beta confirmed in GCP Console.
- Whether folder/org-level project-list visibility permission is missing for current principal path. `?` — resolved: logout/login resolved initial GCP Console scope.
- Whether Firebase is initialized (firebase.projects.addfirebase applied) on Alpha, Gamma, Beta GCP projects. `?` — API enabled, but Firebase API returns `404 NOT_FOUND` for project lookup on all three.
- Whether Firebase Console visibility requires a separate provisioning step or request beyond the current Developer principal grant. `?` — confirmed: API enablement alone is insufficient; Firebase project registration is still missing.
- Exact pending details from James, Marin, and Maria that unblock final closure for Firebase and remaining governance items. `?`

**Evidence**
- Markdown source path confirmed by user and repository artifact.
- `promo-plus-core-infrastructure/` contains Terraform config that can reveal API enablement and IAM bindings.
- `ngps-global/serverless.yml` and related yml resources confirm broad AWS legacy service surface for migration reference.

**Decision**
- Baseline access policy accepted; API-to-role mapping remains pending under James clarified model (GitHub Actions SA predefined roles, Data Migration SA custom-only, GCP Migration Principal custom-only, Developer Principal custom-only).
- Scope Snapshot optimization is pending with operator-first completeness criteria.

**Next step**
- Fill NGPS/Promo+ Product Alpha/Beta/Gamma/Prod rows using the baseline policy as the access control guardrail.
- Expand Scope Snapshot with missing operator foundations, then continue NGPS/Promo+ role mapping.
